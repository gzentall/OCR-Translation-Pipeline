[phases.setup]
nixPkgs = ["python39", "poppler_utils"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pip install gunicorn"]

[start]
cmd = "gunicorn -w 4 -b 0.0.0.0:$PORT --timeout 300 app:app"



